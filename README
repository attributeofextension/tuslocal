README
------
1. In terminal run "docker-compose build"
2. Copy .profile to User home ie "cp .profile ~"
3. Run "docker-compose up"
4. Enter tusweb container with cmd "goinside tusweb"

5. Pull Repos for Server-Side and Client-Side codebases
